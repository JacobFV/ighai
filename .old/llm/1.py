import langchain
import pydantic


class Brain:
    """

    LTM: the vectorstore database
    MTM: the network of concepts that we check for isomorphic subgraphs against
    STM: the LLM's context window

    """


    def update()